# multi-sgb-async

Rust async application for the use and management of multiple RaP!'s single-generator boards

The development is done using Rust's Tokio library

To run, go into the main directory and run the command
```cargo run```
and plug in the board 

Socket reader is to visualize some quick output, simply run the python script on a separate terminal
